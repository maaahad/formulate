import React from "react";
import PageContainer from "@containers/PageContainer";

const Repo = () => {
    return (
        <PageContainer>
            Fallback page for Ship details page
        </PageContainer>
    );
};

export default Repo;